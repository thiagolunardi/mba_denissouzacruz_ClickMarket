<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">ClickMarket</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        @* <div class="nav-item px-3">
            <NavLink class="nav-link" href="login">
                <span class="bi bi-door-open-nav-menu" aria-hidden="true"></span> Login
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="register">
                <span class="bi bi-person-plus-nav-menu" aria-hidden="true"></span> Cadastro
            </NavLink>
        </div> *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="vitrine">
                <span class="bi bi-bag-nav-menu" aria-hidden="true"></span> Vitrine
            </NavLink>
        </div>
        <div class="nav-item px-3" hidden="@(!temToken)">
            <NavLink class="nav-link" href="favoritos">
                <span class="bi bi-heart-fill-nav-menu" aria-hidden="true"></span> Favoritos
            </NavLink>
        </div>
    </nav>
</div>

@code {
    [Inject] IJSRuntime JS { get; set; } = default!;
    private bool temToken;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            var token = await JS.InvokeAsync<string>("localStorage.getItem", "authToken");
            temToken = !string.IsNullOrWhiteSpace(token);
            StateHasChanged();
        }
    }
}


